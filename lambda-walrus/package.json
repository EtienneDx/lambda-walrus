{
  "name": "@etiennedx/lambda-walrus",
  "version": "1.0.0",
  "description": "An AWS lambda utility package to build APIs over lambda using decorators",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "npm run clean && npx tsc",
    "clean": "rimraf dist",
    "test": "jest",
    "autobuild": "npx tsc --watch"
  },
  "keywords": [
    "AWS",
    "Lambda",
    "Handler"
  ],
  "author": "EtienneDx",
  "license": "MIT",
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/aws-lambda": "^8.10.146",
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "rimraf": "^6.0.1",
    "ts-jest": "^29.2.5",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "aws-lambda": "^1.0.7",
    "jsonwebtoken": "^9.0.2",
    "jwks-rsa": "^3.1.0"
  }
}
